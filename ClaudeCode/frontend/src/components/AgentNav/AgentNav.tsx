import { Typography } from "antd";

export default function AgentNav() {
  return (
    <div style={{ padding: 16 }}>
      <Typography.Title level={4}>SeeTime Agent</Typography.Title>
      <Typography.Text type="secondary">Agent 导航区</Typography.Text>
    </div>
  );
}
